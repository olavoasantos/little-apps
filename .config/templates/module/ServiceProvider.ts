export const ServiceProvider: Micra.ServiceProvider = {
  /**
   * Register any <%= _.sentenceCase(name) %> services.
   */
  async register() {
    //
  },

  /**
   * Bootstrap any <%= _.sentenceCase(name) %> services.
   */
  async boot() {
    //
  },
};
